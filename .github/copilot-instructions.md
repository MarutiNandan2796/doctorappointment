# Workspace Instructions

- MERN stack doctor appointment app inspired by Prescripto.
- Keep the UI clean, healthcare-focused, and responsive.
- Use React + Vite in `client` and Express in `server`.
- Prefer minimal, readable code and reusable components.
